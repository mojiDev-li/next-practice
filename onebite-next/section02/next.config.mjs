/** @type {import('next').NextConfig} */

// next앱의 설정을 다루는 곳

const nextConfig = {
  reactStrictMode: false, // react앱에 존재하는 잠재적인 오류를 검사하기 위해 개발 모드로 실행했을때 2번이나 실행함 그래서 꺼줌
};

export default nextConfig;
